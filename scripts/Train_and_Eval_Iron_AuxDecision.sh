#!/bin/bash
# =============================================================================
# 完整流程：训练（z 挨打进化）→ 推理裁决（z 执行裁决）
# 对应「z 的生命周期」：先 Iron.sh 训练辅助头，再 Eval_Iron_AuxDecision.sh 用置信度置零并输出系统决策说明。
# 论文叙事：训练阶段用 BCE 学 z，推理阶段用 z 做裁决，依据均来自训练而非拍脑袋。
# =============================================================================
set -e
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$SCRIPT_DIR/.."

echo "========== 阶段 1：训练（z 挨打进化）=========="
bash scripts/Iron.sh

echo ""
echo "========== 阶段 2：推理裁决（z 执行裁决）=========="
bash scripts/Eval_Iron_AuxDecision.sh

echo ""
echo "========== 完成：训练 + 裁决评估 已跑完 =========="
